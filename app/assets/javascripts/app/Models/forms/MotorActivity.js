;(function (app) {

    app.models.MotorActivityModel = app.models.BaseModel.extend({

        defaults: {},

        wrapperJson: 'motorActivity',

        calculate: function () {

        }

    });

} (app));